//use this file to export all of the modles that we create
const User = require("./User");
const Control = require("./Control");
const Screenings = require("./Screening");

module.exports = { User, Control, Screenings };
